# Run 02 — synthetic first-run walkthrough

**Build `c5b3d62` · 13 August 2026 · frozen**

Same persona, same three tasks, run again after the copy and progress fixes
that followed [run 01](./run-01-ba49d8e.md). Steps whose behaviour is
unchanged from run 01 are summarised rather than re-walked; steps that changed,
and steps that still block, are walked in full.

Screens were driven in a real browser this time (Chromium at 390×844), so the
labels below are what the app rendered, not what the source implies.

---

## Since run 01

| What run 01 found | This build |
| --- | --- |
| "Sign in" / "Log-in" — synonyms, and the primary one created an account | Welcome now reads **Create an account** / **Log in**. The choice names my situation, so there's nothing to guess. ✅ |
| The Auth screen's title contradicted the button that opened it | Tapping Create an account lands on a screen titled "Create an account", submitting says "Create account". Tapping Log in lands on "Log in". ✅ |
| "Search your word" — search what? | Placeholder now **Search a Dutch word**, and the formerly blank screen carries a line: *Look up a Dutch word in the dictionary, then add it to your deck.* Both halves of the question answered before I type. ✅ |
| Deck empty state didn't teach the + button | **"Your deck is empty. Tap + to look up a word and add it here."** ✅ |
| The counter grew on a miss (2/4 → 3/5) | Verified live on a 3-word session: `0/3` at the start, still `0/3` after a miss, then `1/3`, `2/3` as words finish. The denominator never moves. ✅ |

None of the five reintroduced a problem elsewhere. What follows is what this
build does that run 01 couldn't see, plus what it still doesn't do.

---

## Task 1 — capture from reading

Steps 1–5 (open, choose a door, create an account, confirm by email, land on
an empty home) now run without a wrong turn, apart from the email
confirmation, which is unchanged and still interrupts the task. It stays in
the facilitator pre-step.

| # | Step | What happened | |
| --- | --- | --- | --- |
| 2 | Choose between the two buttons | **Create an account** and **Log in**. I have no account; the first one is plainly mine. No hesitation. | ✅ |
| 3 | Tap Create an account | A screen titled "Create an account" — and then, under it, *"Create a **profile** to save your deck and progress across devices."* Three words into the screen the app has renamed the thing I'm making. It's the same object, so I move on, but I noticed. Settings says "Progress syncs to your profile" too. | ⚠️ |
| 4 | Confirm by email | Unchanged from run 01: sign-up can end at "check your inbox… then log in", which sends me out of the app mid-task. Handled by the pre-step, not by the app. | ⚠️ |
| 6 | Get the article into the app | Unchanged. No reader, no paste-a-text, no share target. I read in another tab and retype from memory. | ❌ |
| 7 | Open Add a word | The screen now tells me what it is before I type: *Look up a Dutch word in the dictionary, then add it to your deck.* I know what's being searched and where the word ends up. | ✅ |
| 8 | Type **huurders** | Unchanged. "Did you mean [huurder]" — right answer, no explanation of the relationship. | ⚠️ |
| 11 | Type **gestegen** | Unchanged. [gisteren] [genoegen] [vestigen] — three wrong guesses, correct answer absent, online lookup suppressed behind a link. | ❌ |

**Verdict:** the entry to this task is now clean and the middle of it is
unchanged. The two blockers are both about words, not buttons: nothing carries
the article into the app, and inflected words fall into the suggestion trap.

## Task 2 — capture from a photo

Unchanged in every respect — no code in this area moved between `ba49d8e` and
`c5b3d62`. No camera, no gallery picker, no OCR, no permission step. The task
still has no path, and the typed fallback still returns "attack / assault" for
`aanslag` and "to soak" for `weken` as single, uneditable meanings.

One thing the clearer capture screen does change: the new start-state line
promises the **dictionary**, and a word that isn't in it — `huurtoeslag`, say —
is still found, silently, online. That's a better outcome than the promise, but
it does mean the line under-describes what the screen can do, and a
participant who reads it may not try long compounds at all.

**Verdict:** ❌, unchanged. Re-script as a typed task until capture-from-image
ships.

## Task 3 — practice

| # | Step | What happened | |
| --- | --- | --- | --- |
| 1–2 | Start a session | Unchanged and good. "You have 3 words ready to review in your deck." → flashcard. | ✅ |
| 3 | Work out whether to flip first | Unchanged. Both grade buttons are live before the flip and nothing states the intended order. | ⚠️ |
| 6 | Miss a card | **Fixed.** The counter holds at `0/3` and the bar doesn't move; later the word returns under "One more try". The session no longer appears to grow as a punishment. | ✅ |
| — | Read the counter cold | New, minor: it now starts at `0/3` because it counts words *finished*. That's honest and matches the bar beside it, but "0" on the first card could be read as a position rather than a total banked. Worth watching in Study 2 — not worth changing on a guess. | ⚠️ |
| 8–9 | Leave after missing a word | **Unchanged, and still the worst thing in the app.** The summary offers only "Review missed words" — no dashboard button, no close, no tab bar. Missing again reproduces the same screen. The exit is answering "I knew it" first-attempt for every word I've just said I don't know, or reloading the page. | ❌ |
| 10 | Quit halfway with the × | Unchanged. Every grade in the session is discarded silently. | ❌ |

**Verdict:** the fix landed cleanly and the loop it was blamed for is gone. The
exit is still a trap, and it is now unambiguously the top blocker for Study 2.

---

## New this run

| Finding | Detail |
| --- | --- |
| **"Account" and "profile" are the same thing under two names** | The create screen is titled "Create an account", its button says "Create account", and its own subtitle says "Create a profile". Settings repeats "profile". Fixed in the commit that adds this run — both now say "account". |
| **The capture hint under-describes the search** | "Look up a Dutch word in the dictionary" is true for the bundled 14k, but a miss with no near-match also queries Wiktionary. Participants told "the dictionary" may not attempt compounds like `huurtoeslag`. Low priority; revisit if Study 2 shows people avoiding long words. |
| **`0/N` at session start** | A consequence of counting finished words rather than position. Observe rather than pre-emptively change. |

## Still open

Carried forward from run 01, unchanged on this build:

- No route from reading into the app (T1).
- No photo or OCR capture (T2).
- Silent wrong senses, with no way to edit a saved translation (T2).
- Inflected forms miss, and a near-match suppresses the online lookup (T1, T2).
- The summary screen loops when a session leaves missed words (T3).
- Closing a session discards its grades silently (T3).
- No password recovery on the Log in screen.
- Sign-up can end at an email confirmation, mid-task.
- "Add another" doesn't exist — every save returns to the deck.

---

## Revised task script

Unchanged from run 01 except the pre-step, which now matches the app's own
words. This is the version in force for Study 2.

**Facilitator pre-step.** Have the participant open `<URL>` in their phone
browser and tap **Create an account**, completing any email confirmation,
before the first task begins. Confirm they land on a screen saying "Add your
first word". If confirmation email is switched on, warn them it's coming.

**Task 1 — capture while reading.** *Here is a short Dutch news article, on
paper / in a second browser tab. Read it as you normally would. When you reach
a word you don't know and would want to remember, add that word to Woordkast,
then keep going. Add two or three words this way. Think aloud — especially if
the app shows you something you didn't expect.*

**Task 2 — capture from something in your life.** *Here is a letter from the
gemeente / a photo of a street sign. Pick out three words you don't recognise
and add them to Woordkast. When you see the meaning the app gives you, tell me
whether it matches what you think the word means here.*

**Task 3 — practice what you saved.** *Practise the words you've added. Work
through them the way you would on a normal day, and stop when you feel you're
done for now. Then tell me what you'd do next.*

Keep the recovery instruction ready for Task 3 — "reload the page" — and log
every time you have to use it. Until the summary screen has an exit, that log
is the measurement.

---

## Explicitly out of scope for this report

No completion rate, no task timing, and no usability or satisfaction
judgement. This run only validates that the script and happy path are
logically coherent. Real friction is measured in Study 2.

---

## Appendix — fixed since this run

Status only. The findings above describe build `c5b3d62` and are not edited.

| Finding | Status | Where |
| --- | --- | --- |
| Summary screen loops when a session leaves missed words | Fixed — "Back to dashboard" is always offered, as a link beside other actions or as the primary button when there are none | `43a1fdd` |
| Closing a session discards its grades silently | Fixed — every answer is written as it's given, so an abandoned session keeps what was answered | `43a1fdd` |
| Sign-up can end at an email confirmation, mid-task | Fixed as a step — "Check your email", with resend and a way back to log in. The trip to an inbox remains inherent to email confirmation | `43a1fdd` |
| No password recovery on the Log in screen | Fixed — "Forgot your password?" sends a link, and following it lands on "Set a new password" | `43a1fdd` |
| The script called the app a "mobile app" | Fixed in the protocol — every script version must hand over a URL and say there's nothing to install | `43a1fdd` |
| "Account" and "profile" used interchangeably | Fixed in the commit that added this run — both say "account" | `70ab3c6` |

Still open after run 03: no route from reading into the app, no photo capture,
silent wrong senses, inflected-form lookup, pre-flip grading, and no "add
another".
