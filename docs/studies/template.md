# Run NN — synthetic first-run walkthrough

**Build `<short-sha>` · <date> · frozen**

<One paragraph: who the participant is, what they attempted, and anything that
had to be substituted (an unreachable article, a missing artefact). Say what
was driven in a browser and what was read from source.>

---

## Since the last run

<Table: each finding the previous run raised that this build claims to
address, and what actually happened when it was attempted again. This is the
section the reader wants first — everything else is context for it.>

| What run <NN-1> found | This build |
| --- | --- |
|  |  |

---

## Task 1 — capture from reading

<Walk changed and still-blocking steps in full; summarise the rest in a line.
Rate each ✅ / ⚠️ / ❌. Say what was expected before each action and what
actually happened after it — a step with no expectation recorded can't be
judged later.>

| # | Step | What happened | |
| --- | --- | --- | --- |
|  |  |  |  |

**Verdict:**

## Task 2 — capture from a photo

## Task 3 — practice

---

## New this run

<Only what this build introduced or what the previous run couldn't see. If a
finding was fixed in the same commit that adds this run, say so here rather
than deleting it — the sequence is part of the record.>

## Still open

<Flat list carried forward, unchanged, so a reader of this file alone knows
the full state without opening earlier runs.>

---

## Revised task script

<Full script, self-contained — the next run and Study 2 both read the most
recent run's version, so it can't be a diff against an older file. Note what
changed from the previous run and why.>

---

## Explicitly out of scope for this report

No completion rate, no task timing, and no usability or satisfaction
judgement. This run only validates that the script and happy path are
logically coherent. Real friction is measured with participants.
